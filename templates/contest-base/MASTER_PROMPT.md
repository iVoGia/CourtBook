# AI ADAPT BATTLE — Session thi 60 phút

## Framework đang dùng

**{FRAMEWORK_NAME}**

## Đề bài bốc thăm

```
{DE_BAI}
```

## Quy tắc bắt buộc

1. Follow đúng workflow trong [`CONTEST_WORKFLOW.md`](CONTEST_WORKFLOW.md) của folder này
2. Scope MVP — 1 flow demo end-to-end trong 60 phút
3. UI: dark/warm hoặc theme phù hợp đề; Tailwind + component library (xem `assets/ui-kit.md`)
4. Ảnh: dùng `assets/images.md`; mỗi item/card phải có ảnh hoặc placeholder đẹp
5. Mọi button/form phải hoạt động — không dead click
6. Phút 50: chạy Presentation Agent → tạo `presentation/output.pptx`
7. Tick từng bước trong `FRAMEWORK_CHECKLIST.md` khi hoàn thành

## Stack mặc định (trừ khi đề bắt buộc khác)

- **Web:** Vite + React + TS + Tailwind + shadcn-style components
- **API:** Express + SQLite HOẶC mock JSON nếu hết giờ
- **Chạy:** `npm run dev`

## Output bắt buộc cuối phiên

- [ ] App chạy local, demo được
- [ ] README ngắn: cách chạy
- [ ] `presentation/output.pptx` (5 phút thuyết trình)
- [ ] `FRAMEWORK_CHECKLIST.md` đã tick từng bước framework

**Bắt đầu Phase 1 theo framework. KHÔNG code trước khi xong bước spec/plan của framework.**
