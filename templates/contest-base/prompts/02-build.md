# Prompt 02 — Build (phút 15–45)

Paste sau khi đã có plan + scaffold:

```
UI requirements (bắt buộc — ăn 40 điểm sản phẩm):

Đọc assets/ui-kit.md và assets/images.md trước khi code UI.

- Layout: sidebar + main (desktop), bottom nav hoặc hamburger (mobile 375px)
- Mỗi list/grid item có ảnh 16:9, rounded-xl, object-cover
- Ảnh: lấy từ assets/images.md theo category; onError → public/images/placeholder.svg
- Mọi CTA phải có handler thật (loading state, toast/error nếu API fail)
- Empty state có text/illustration — không để màn trống
- Spacing nhất quán: p-4, gap-4, rounded-xl cards
- Không dùng HTML thuần không style
- Theme: stone-950 bg + amber-500 accent (hoặc phù hợp đề)

Implement theo plan. Ưu tiên 1 flow demo end-to-end hoàn chỉnh trước khi thêm feature phụ.
```
