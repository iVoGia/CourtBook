# Demo Accounts — CourtBook

Password for all demo accounts: **demo123**

| Email | Role | Use case |
|-------|------|----------|
| user@demo.com | user | Book courts, view/cancel bookings |
| admin@demo.com | admin | Same as user (admin CRUD stretch) |

Seeded via `npm run db:init` in `server/src/db/seed.sql`.
