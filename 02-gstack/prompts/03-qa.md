# Prompt 03 — QA full-stack (phút 36–44)

```
QA gstack full-stack — web phải PASS trước Flutter gate:

1. /design-review — visual polish + responsive
2. /review — code + API review
3. /qa http://localhost:5173 — gstack browse

Checklist:
1. npm run db:init && npm run dev — client :5173 + API :3001
2. GET /api/health — OK
3. Flow chính end-to-end qua UI → API → SQLite
4. Refresh browser — data vẫn còn (persistence)
5. **Responsive 375px** — no horizontal scroll, bottom nav OK, screenshot mobile
6. **Desktop 1280px** — sidebar layout OK, screenshot desktop
7. Login (nếu có): demo account từ docs/DEMO_ACCOUNTS.md hoạt động
8. docs/ khớp code — chạy `bash ../scripts/verify-docs.sh app`
9. Điền **docs/QA_REPORT.md** (pass/fail từng mục)
10. UI khớp DESIGN.md

Lưu verify output → docs/FRAMEWORK_EVIDENCE/verify-docs.log

Screenshots:
- presentation/screenshots/01-dashboard.png (desktop)
- presentation/screenshots/02-feature.png (feature / mobile 375)
- presentation/screenshots/03-result.png (flow kết quả)

Tick FRAMEWORK_CHECKLIST Phase 5.

Sau khi PASS → paste prompts/03c-flutter-gate.md (hỏi user có gen Flutter không).
```
